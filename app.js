const translations = {
  en: {
    active: "Active",
    activeRequests: "Active Requests",
    aiForecast: "AI Forecast",
    aiHelp: "AI Help",
    aiMatch: "AI Match",
    available: "Available",
    bestDonors: "Best Donors",
    bloodGroup: "Blood Group",
    bloodNeededNow: "Blood Needed Now",
    bloodStock: "Blood Stock",
    canDonateToday: "Can Donate Today",
    chatDefault: "Choose a question.",
    contact: "Contact",
    continueGoogle: "Continue With Google",
    critical: "Critical",
    criticalTwoUnits: "Critical · 2 units · 1.4 km",
    donateBlood: "Donate Blood",
    donateBloodHi: "खून देना है",
    donationHistory: "Donation History",
    donor: "Donor",
    eligibility: "Eligibility",
    emergency: "Emergency",
    eta: "ETA",
    fraudCheck: "Fraud Check",
    fraudText: "2 reward claims need review",
    fulfilled: "Fulfilled",
    gpsReady: "GPS ready",
    group: "Group",
    healthCare: "Healthcare",
    hospitalBank: "Hospital + Blood Bank",
    hospitalDesk: "Hospital Desk",
    hospitalDeskHi: "अस्पताल",
    hospitalShort: "Hospital",
    jwtSecure: "JWT Secure",
    lastDonation: "Last donation: 70 days ago",
    leaderboard: "Leaderboard",
    listen: "Listen",
    loginSubtext: "Use one Gmail account to reach donors, hospitals, blood banks, and rewards.",
    loginSuccess: "Signed in with Google",
    loginWithGoogle: "Login With Google",
    live: "Live",
    liveControl: "Live Control",
    low: "Low",
    lowThreeUnits: "Low · 3 units · 3.0 km",
    logout: "Logout",
    logoutSuccess: "Logged out",
    markMatched: "Mark matched",
    matched: "Matched",
    moderate: "Moderate",
    moderateOneUnit: "Moderate · 1 unit · 2.8 km",
    monthly: "Monthly",
    monthlyRank: "Monthly rank: 4",
    myLocation: "My Location",
    nearbyRequests: "Nearby Requests",
    needBlood: "Need Blood",
    needBloodHi: "खून चाहिए",
    nextCamp: "Next camp: 18 June",
    nextSevenDays: "7 days",
    notifyDonors: "Notify donors",
    open: "Open",
    patient: "Patient",
    place: "Place",
    points: "Points",
    pointsCare: "Points + Care",
    qAgain: "When again?",
    qEat: "What should I eat?",
    qEligible: "Can I donate today?",
    qGroups: "Which groups?",
    rareWatch: "Rare watch",
    readyToSave: "Ready To Save",
    redeem: "Redeem",
    reminder: "Reminder",
    rewards: "Rewards",
    rewardsHelp: "Rewards & Help",
    rewardsHelpHi: "इनाम और सहायता",
    safeAccess: "Safe Access",
    secureGoogle: "Secure Google sign in",
    sendAlert: "Send Alert",
    shortageSoon: "Shortage soon",
    sms: "SMS",
    smsAlerts: "SMS alerts ready",
    smsOn: "SMS On",
    status: "Status",
    steady: "Steady",
    tagline: "Blood Bridge",
    unitsNeeded: "Units Needed",
    urgency: "Urgency",
    verified: "Verified"
  },
  hi: {
    active: "चालू",
    activeRequests: "चालू मांग",
    aiForecast: "AI अनुमान",
    aiHelp: "AI सहायता",
    aiMatch: "AI मिलान",
    available: "तैयार",
    bestDonors: "सही दाता",
    bloodGroup: "खून समूह",
    bloodNeededNow: "अभी खून चाहिए",
    bloodStock: "खून स्टॉक",
    canDonateToday: "आज दे सकते हैं",
    chatDefault: "सवाल चुनें।",
    contact: "मोबाइल",
    continueGoogle: "Google से आगे बढ़ें",
    critical: "बहुत जरूरी",
    criticalTwoUnits: "बहुत जरूरी · 2 यूनिट · 1.4 km",
    donateBlood: "खून दें",
    donateBloodHi: "Donate Blood",
    donationHistory: "दान इतिहास",
    donor: "दाता",
    eligibility: "योग्यता",
    emergency: "आपातकाल",
    eta: "समय",
    fraudCheck: "सुरक्षा जांच",
    fraudText: "2 इनाम दावे जांच में",
    fulfilled: "पूरा",
    gpsReady: "GPS तैयार",
    group: "समूह",
    healthCare: "स्वास्थ्य सेवा",
    hospitalBank: "अस्पताल + ब्लड बैंक",
    hospitalDesk: "अस्पताल",
    hospitalDeskHi: "Hospital Desk",
    hospitalShort: "अस्पताल",
    jwtSecure: "सुरक्षित",
    lastDonation: "पिछला दान: 70 दिन पहले",
    leaderboard: "शीर्ष दाता",
    listen: "सुनें",
    loginSubtext: "एक Gmail खाते से दाता, अस्पताल, ब्लड बैंक और इनाम तक पहुंचें।",
    loginSuccess: "Google से लॉगिन हुआ",
    loginWithGoogle: "Google से लॉगिन",
    live: "लाइव",
    liveControl: "लाइव नियंत्रण",
    low: "कम",
    lowThreeUnits: "कम · 3 यूनिट · 3.0 km",
    logout: "बाहर जाएं",
    logoutSuccess: "लॉगआउट हुआ",
    markMatched: "मिल गया",
    matched: "मिल गया",
    moderate: "मध्यम",
    moderateOneUnit: "मध्यम · 1 यूनिट · 2.8 km",
    monthly: "मासिक",
    monthlyRank: "मासिक रैंक: 4",
    myLocation: "मेरी जगह",
    nearbyRequests: "पास की मांग",
    needBlood: "खून चाहिए",
    needBloodHi: "Need Blood",
    nextCamp: "अगला कैंप: 18 जून",
    nextSevenDays: "7 दिन",
    notifyDonors: "दाता बुलाएं",
    open: "खुला",
    patient: "मरीज",
    place: "जगह",
    points: "अंक",
    pointsCare: "अंक + देखभाल",
    qAgain: "फिर कब?",
    qEat: "क्या खाएं?",
    qEligible: "आज दे सकता हूं?",
    qGroups: "कौन सा समूह?",
    rareWatch: "दुर्लभ ध्यान",
    readyToSave: "मदद के लिए तैयार",
    redeem: "इनाम लें",
    reminder: "याद दिलाना",
    rewards: "इनाम",
    rewardsHelp: "इनाम और सहायता",
    rewardsHelpHi: "Rewards & Help",
    safeAccess: "सुरक्षित प्रवेश",
    secureGoogle: "सुरक्षित Google लॉगिन",
    sendAlert: "अलर्ट भेजें",
    shortageSoon: "जल्द कमी",
    sms: "SMS",
    smsAlerts: "SMS अलर्ट तैयार",
    smsOn: "SMS चालू",
    status: "स्थिति",
    steady: "ठीक",
    tagline: "Blood Bridge",
    unitsNeeded: "यूनिट",
    urgency: "जरूरत",
    verified: "जांचा हुआ"
  }
};

const bloodGroups = ["A+", "A-", "B+", "B-", "AB+", "AB-", "O+", "O-"];

const compatibility = {
  "O-": ["O-"],
  "O+": ["O-", "O+"],
  "A-": ["O-", "A-"],
  "A+": ["O-", "O+", "A-", "A+"],
  "B-": ["O-", "B-"],
  "B+": ["O-", "O+", "B-", "B+"],
  "AB-": ["O-", "A-", "B-", "AB-"],
  "AB+": ["O-", "O+", "A-", "A+", "B-", "B+", "AB-", "AB+"]
};

const donors = [
  { name: "Asha K.", group: "O-", distance: 1.4, phone: "+91 90000 12341", eligible: "Today", score: 98 },
  { name: "Rahul S.", group: "O-", distance: 2.1, phone: "+91 90000 12342", eligible: "Today", score: 94 },
  { name: "Meera P.", group: "A-", distance: 2.8, phone: "+91 90000 12343", eligible: "Today", score: 88 },
  { name: "Imran H.", group: "O+", distance: 3.2, phone: "+91 90000 12344", eligible: "Today", score: 86 },
  { name: "Kavya R.", group: "B-", distance: 4.6, phone: "+91 90000 12345", eligible: "Today", score: 82 },
  { name: "Dev M.", group: "A+", distance: 5.1, phone: "+91 90000 12346", eligible: "Today", score: 79 },
  { name: "Sara N.", group: "AB-", distance: 6.4, phone: "+91 90000 12347", eligible: "Today", score: 76 },
  { name: "Vikram T.", group: "B+", distance: 6.8, phone: "+91 90000 12348", eligible: "Today", score: 73 }
];

const inventory = {
  "A+": 18,
  "A-": 7,
  "B+": 14,
  "B-": 5,
  "AB+": 9,
  "AB-": 3,
  "O+": 21,
  "O-": 4
};

const chatAnswers = {
  en: {
    eligible: "You can donate if you are healthy, 18 or older, and your last donation was more than 56 days ago.",
    eat: "Eat a light meal, drink water, and avoid alcohol before donation.",
    again: "Most donors can donate again after 56 days.",
    compatibility: "O negative can donate to everyone. AB positive can receive from everyone."
  },
  hi: {
    eligible: "अगर आप स्वस्थ हैं, 18 वर्ष से ऊपर हैं, और पिछला दान 56 दिन से पहले हुआ है, तो आप दान कर सकते हैं।",
    eat: "हल्का खाना खाएं, पानी पिएं, और दान से पहले शराब न लें।",
    again: "अधिकतर दाता 56 दिन बाद फिर दान कर सकते हैं।",
    compatibility: "O negative सबको दे सकता है। AB positive सबका खून ले सकता है।"
  }
};

const state = {
  activePanel: "request",
  bloodGroup: "O-",
  units: 2,
  urgency: "critical",
  location: "City Care Hospital",
  language: "en",
  voiceOn: false,
  requestMatched: false,
  available: true,
  signedIn: false,
  loginRole: "request"
};

const $ = (selector, root = document) => root.querySelector(selector);
const $$ = (selector, root = document) => Array.from(root.querySelectorAll(selector));

function iconUse(name) {
  const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
  const use = document.createElementNS("http://www.w3.org/2000/svg", "use");
  use.setAttribute("href", `#${name}`);
  svg.appendChild(use);
  return svg;
}

function applyLanguage() {
  document.documentElement.lang = state.language === "hi" ? "hi" : "en";
  $$("[data-i18n]").forEach((node) => {
    const key = node.dataset.i18n;
    if (translations[state.language][key]) {
      node.textContent = translations[state.language][key];
    }
  });
  $$(".language-toggle span").forEach((node) => {
    node.textContent = state.language === "en" ? "हिंदी" : "EN";
  });
  $("#availabilityToggle span").textContent =
    state.available
      ? translations[state.language].available
      : state.language === "hi"
        ? "आराम"
        : "Resting";
  renderMatches();
}

function speak(text) {
  if (!("speechSynthesis" in window)) {
    showToast(state.language === "hi" ? "आवाज उपलब्ध नहीं" : "Voice is not available");
    return;
  }
  window.speechSynthesis.cancel();
  const utterance = new SpeechSynthesisUtterance(text);
  utterance.lang = state.language === "hi" ? "hi-IN" : "en-IN";
  utterance.rate = 0.88;
  window.speechSynthesis.speak(utterance);
}

function activePanelText() {
  const label = translations[state.language];
  const summaries = {
    request:
      state.language === "hi"
        ? `${label.bloodNeededNow}. ${state.bloodGroup}, ${state.units} यूनिट, ${label[state.urgency]}. ${label.sendAlert}.`
        : `${label.bloodNeededNow}. ${state.bloodGroup}, ${state.units} units, ${label[state.urgency]}. ${label.sendAlert}.`,
    donor:
      state.language === "hi"
        ? `${label.readyToSave}. ${label.canDonateToday}. ${label.points}: 1450.`
        : `${label.readyToSave}. ${label.canDonateToday}. ${label.points}: 1450.`,
    hospital:
      state.language === "hi"
        ? `${label.liveControl}. ${label.bloodStock}. ${label.aiForecast}.`
        : `${label.liveControl}. ${label.bloodStock}. ${label.aiForecast}.`,
    rewards:
      state.language === "hi"
        ? `${label.pointsCare}. ${label.redeem}. ${label.aiHelp}.`
        : `${label.pointsCare}. ${label.redeem}. ${label.aiHelp}.`
  };
  return summaries[state.activePanel];
}

function loginScreenText() {
  return state.language === "hi"
    ? `${translations.hi.loginWithGoogle}. ${translations.hi[state.loginRole === "request" ? "needBlood" : state.loginRole === "donor" ? "donor" : "hospitalShort"]}.`
    : `${translations.en.loginWithGoogle}. ${translations.en[state.loginRole === "request" ? "needBlood" : state.loginRole === "donor" ? "donor" : "hospitalShort"]}.`;
}

function showPanel(panelName) {
  state.activePanel = panelName;
  $$(".role-card").forEach((button) => {
    const isActive = button.dataset.panel === panelName;
    button.classList.toggle("active", isActive);
    button.setAttribute("aria-current", isActive ? "page" : "false");
  });
  $$(".panel").forEach((panel) => {
    panel.classList.toggle("active", panel.dataset.panel === panelName);
  });
  if (state.voiceOn) speak(activePanelText());
}

function setLoginRole(role) {
  state.loginRole = role;
  $$(".login-role").forEach((button) => {
    const isActive = button.dataset.loginRole === role;
    button.classList.toggle("active", isActive);
    button.setAttribute("aria-pressed", isActive ? "true" : "false");
  });
  if (state.voiceOn) {
    const activeButton = $(`.login-role[data-login-role="${role}"]`);
    speak(activeButton.dataset[state.language === "hi" ? "speakHi" : "speakEn"]);
  }
}

function signInWithGoogle() {
  state.signedIn = true;
  $("#loginScreen").classList.add("hidden");
  $("#appShell").classList.remove("hidden");
  $("#appShell").removeAttribute("aria-hidden");
  showPanel(state.loginRole);
  showToast(translations[state.language].loginSuccess);
  if (state.voiceOn) speak(translations[state.language].loginSuccess);
}

function signOut() {
  state.signedIn = false;
  $("#appShell").classList.add("hidden");
  $("#appShell").setAttribute("aria-hidden", "true");
  $("#loginScreen").classList.remove("hidden");
  showToast(translations[state.language].logoutSuccess);
  if (state.voiceOn) speak(translations[state.language].logoutSuccess);
}

function renderBloodButtons() {
  const grid = $("#bloodGroupGrid");
  grid.replaceChildren();
  bloodGroups.forEach((group) => {
    const button = document.createElement("button");
    button.className = "blood-button";
    button.type = "button";
    button.textContent = group;
    button.setAttribute("aria-label", `Blood group ${group}`);
    button.classList.toggle("active", state.bloodGroup === group);
    button.addEventListener("click", () => {
      state.bloodGroup = group;
      state.requestMatched = false;
      $("#requestStatus").textContent = "OPEN";
      $("#matchedStep").classList.remove("done");
      $("#fulfilledStep").classList.remove("done");
      renderBloodButtons();
      renderMatches();
      if (state.voiceOn) speak(`${group}`);
    });
    grid.appendChild(button);
  });
}

function compatibleDonors() {
  const allowed = compatibility[state.bloodGroup] || [state.bloodGroup];
  return donors
    .filter((donor) => allowed.includes(donor.group))
    .sort((a, b) => b.score - a.score || a.distance - b.distance)
    .slice(0, 5);
}

function renderMatches() {
  const matches = compatibleDonors();
  const list = $("#matchList");
  list.replaceChildren();
  $("#matchCount").textContent = matches.length;
  $("#smsCount").textContent = String(matches.length * 4 + state.units * 2);

  matches.forEach((donor) => {
    const row = document.createElement("article");
    row.className = "donor-row";

    const avatar = document.createElement("span");
    avatar.className = "donor-avatar";
    avatar.textContent = donor.group;

    const details = document.createElement("div");
    const name = document.createElement("strong");
    name.textContent = donor.name;
    const meta = document.createElement("small");
    meta.textContent =
      state.language === "hi"
        ? `${donor.distance.toFixed(1)} km · आज तैयार · स्कोर ${donor.score}`
        : `${donor.distance.toFixed(1)} km · eligible today · score ${donor.score}`;
    details.append(name, meta);

    const call = document.createElement("button");
    call.type = "button";
    call.setAttribute("aria-label", `Call ${donor.name}`);
    call.dataset.actionToast = state.language === "hi" ? "कॉल तैयार" : "Call ready";
    call.appendChild(iconUse("icon-phone"));

    row.append(avatar, details, call);
    list.appendChild(row);
  });
}

function renderInventory() {
  const grid = $("#inventoryGrid");
  grid.replaceChildren();
  Object.entries(inventory).forEach(([group, count]) => {
    const cell = document.createElement("article");
    cell.className = "stock-cell";
    cell.classList.toggle("low", count < 6);

    const groupEl = document.createElement("strong");
    groupEl.textContent = group;
    const countEl = document.createElement("output");
    countEl.textContent = count;
    countEl.setAttribute("aria-label", `${group} stock ${count}`);

    const actions = document.createElement("div");
    actions.className = "stock-actions";

    const minus = document.createElement("button");
    minus.type = "button";
    minus.textContent = "-";
    minus.setAttribute("aria-label", `Remove one ${group} unit`);
    minus.addEventListener("click", () => updateStock(group, -1));

    const plus = document.createElement("button");
    plus.type = "button";
    plus.textContent = "+";
    plus.setAttribute("aria-label", `Add one ${group} unit`);
    plus.addEventListener("click", () => updateStock(group, 1));

    actions.append(minus, plus);
    cell.append(groupEl, countEl, actions);
    grid.appendChild(cell);
  });
}

function updateStock(group, change) {
  inventory[group] = Math.max(0, inventory[group] + change);
  renderInventory();
  const message =
    state.language === "hi"
      ? `${group} स्टॉक ${inventory[group]}`
      : `${group} stock ${inventory[group]}`;
  showToast(message);
}

function setUnits(change) {
  state.units = Math.min(12, Math.max(1, state.units + change));
  $("#unitOutput").textContent = state.units;
  if (state.voiceOn) {
    speak(state.language === "hi" ? `${state.units} यूनिट` : `${state.units} units`);
  }
}

function setUrgency(urgency) {
  state.urgency = urgency;
  $$("#urgencyGroup button").forEach((button) => {
    button.classList.toggle("active", button.dataset.urgency === urgency);
  });
  if (state.voiceOn) {
    speak(translations[state.language][urgency]);
  }
}

function setLocation(location) {
  state.location = location;
  $$(".location-choice").forEach((button) => {
    button.classList.toggle("active", button.dataset.location === location);
  });
}

function broadcastAlert() {
  state.requestMatched = true;
  $("#requestStatus").textContent = "MATCHED";
  $("#matchedStep").classList.add("done");
  renderMatches();
  const message =
    state.language === "hi"
      ? `${state.bloodGroup} के लिए अलर्ट भेजा गया। दाता मिल गए।`
      : `Alert sent for ${state.bloodGroup}. Donors matched.`;
  showToast(message);
  speak(message);
}

function toggleAvailability() {
  state.available = !state.available;
  const button = $("#availabilityToggle");
  button.classList.toggle("active", state.available);
  button.querySelector("span").textContent =
    state.available
      ? translations[state.language].available
      : state.language === "hi"
        ? "आराम"
        : "Resting";
  showToast(
    state.available
      ? state.language === "hi"
        ? "आप तैयार हैं"
        : "You are available"
      : state.language === "hi"
        ? "आप आराम में हैं"
        : "You are resting"
  );
}

function answerQuestion(key) {
  $$(".quick-questions button").forEach((button) => {
    button.classList.toggle("active", button.dataset.question === key);
  });
  const answer = chatAnswers[state.language][key];
  const answerBox = $("#chatAnswer");
  answerBox.querySelector("p").textContent = answer;
  if (state.voiceOn) speak(answer);
}

let toastTimer;
function showToast(message) {
  const toast = $("#toast");
  toast.textContent = message;
  toast.classList.add("show");
  window.clearTimeout(toastTimer);
  toastTimer = window.setTimeout(() => toast.classList.remove("show"), 2400);
}

function toggleLanguage() {
  state.language = state.language === "en" ? "hi" : "en";
  applyLanguage();
  renderInventory();
  showToast(state.language === "hi" ? "हिंदी चालू" : "English on");
}

function toggleVoice(button) {
  state.voiceOn = !state.voiceOn;
  $$("#voiceToggle, #loginVoiceButton").forEach((voiceButton) => {
    voiceButton.classList.toggle("active", state.voiceOn);
  });
  const message = state.voiceOn
    ? state.language === "hi"
      ? "आवाज चालू"
      : "Voice on"
    : state.language === "hi"
      ? "आवाज बंद"
      : "Voice off";
  showToast(message);
  if (state.voiceOn) speak(state.signedIn ? activePanelText() : loginScreenText());
}

function bindEvents() {
  $$(".login-role").forEach((button) => {
    button.addEventListener("click", () => setLoginRole(button.dataset.loginRole));
  });

  $("#googleLoginButton").addEventListener("click", signInWithGoogle);
  $("#logoutButton").addEventListener("click", signOut);

  $$(".role-card").forEach((button) => {
    button.addEventListener("click", () => {
      showPanel(button.dataset.panel);
      if (state.voiceOn) {
        speak(button.dataset[state.language === "hi" ? "speakHi" : "speakEn"]);
      }
    });
  });

  $("#languageToggle").addEventListener("click", toggleLanguage);
  $("#loginLanguageToggle").addEventListener("click", toggleLanguage);

  $("#voiceToggle").addEventListener("click", (event) => toggleVoice(event.currentTarget));
  $("#loginVoiceButton").addEventListener("click", (event) => toggleVoice(event.currentTarget));

  $("#minusUnits").addEventListener("click", () => setUnits(-1));
  $("#plusUnits").addEventListener("click", () => setUnits(1));

  $$("#urgencyGroup button").forEach((button) => {
    button.addEventListener("click", () => setUrgency(button.dataset.urgency));
  });

  $$(".location-choice").forEach((button) => {
    button.addEventListener("click", () => setLocation(button.dataset.location));
  });

  $("#broadcastButton").addEventListener("click", broadcastAlert);
  $("#availabilityToggle").addEventListener("click", toggleAvailability);

  $$(".quick-questions button").forEach((button) => {
    button.addEventListener("click", () => answerQuestion(button.dataset.question));
  });

  document.addEventListener("click", (event) => {
    const button = event.target.closest("[data-action-toast]");
    if (!button) return;
    showToast(button.dataset.actionToast);
    if (state.voiceOn) speak(button.dataset.actionToast);
  });
}

function init() {
  setLoginRole(state.loginRole);
  renderBloodButtons();
  renderMatches();
  renderInventory();
  applyLanguage();
  bindEvents();
}

init();
